Created At: 2026-07-13T16:38:48Z
Completed At: 2026-07-13T16:38:57Z
The following changes were made by the multi_replace_file_content tool to: c:\laragon\www\inkscod\kurmon\src\pages\kedisiplinan\RekapKedisiplinan.jsx. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -5,6 +5,7 @@
 import useAuthStore from '../../store/monitoring/authStore.js';
 import * as XLSX from 'xlsx';
 import jsPDF from 'jspdf';
+import { loadInitialState } from '../../utils/state.js';
 
 export default function RekapKedisiplinan({ classes = [], students = [] }) {
   const [riwayat, setRiwayat] = useState([]);
@@ -18,6 +18,11 @@
   const [raporPaperSize, setRaporPaperSize] = useState('A4'); // 'A4' | 'F4'
   const [activeSection, setActiveSection] = useState('siswa'); // 'siswa' | 'kelas' | 'leaderboard'
 
+  const appSettings = useMemo(() => {
+    const defaults = { kopSuratLogo: "", kopSuratBaris1: "", kopSuratBaris2: "", kopSuratBaris3: "", primaryColor: "var(--ui-primary)" };
+    return { ...defaults, ...loadInitialState("appSettings", defaults) };
+  }, []);
+
   const showToast = (message, type = 'success') => {
     setToast({ message, type });
     setTimeout(() => setToast(null), 3500);
@@ -228,7 +228,7 @@
     showToast('Rekap Excel berhasil diunduh!');
   };
 
-  const printRaporPDF = (student, size = 'A4') => {
+  const printRaporPDF = (student, size = 'A4', action = 'download') => {
     const doc = new jsPDF({
       orientation: 'portrait',
       unit: 'mm',
@@ -239,10 +239,27 @@
     const studentClass = student.class_name || student.kelas || "";
     const studentNis = student.nis || student.code || "";
 
-    // Header
+    let yPos = 20;
+
+    // Header Kop Surat
+    if (appSettings.kopSuratLogo) {
+      doc.addImage(appSettings.kopSuratLogo, 'PNG', 15, 10, 25, 25);
+      doc.setFont("Helvetica", "bold");
+      doc.setFontSize(10);
+      doc.text(appSettings.kopSuratBaris1 || "", pageWidth / 2, 16, { align: "center" });
+      doc.se
<truncated 24273 bytes>
 <button type="button" onClick={() => setRaporPaperSize('A4')}
-                    className={`py-3 text-center rounded-[var(--ui-radius-small)] font-bold text-sm border transition-all cursor-pointer ${
-                      raporPaperSize === 'A4' ? 'bg-rose-50 text-rose-600 border-rose-500' : 'bg-white text-slate-600 border-slate-200 hover:bg-slate-50'
-                    }`}>
-                    A4 (Standar)
-                  </button>
-                  <button type="button" onClick={() => setRaporPaperSize('F4')}
-                    className={`py-3 text-center rounded-[var(--ui-radius-small)] font-bold text-sm border transition-all cursor-pointer ${
-                      raporPaperSize === 'F4' ? 'bg-rose-50 text-rose-600 border-rose-500' : 'bg-white text-slate-600 border-slate-200 hover:bg-slate-50'
-                    }`}>
-                    F4 (Folio/HVS)
-                  </button>
-                </div>
-              </div>
-              <div className="pt-2 flex justify-end gap-2">
-                <Button type="button" onClick={() => setSelectedStudentForRapor(null)} className="bg-slate-100 text-slate-600 hover:bg-slate-200 border-none">
-                  Batal
-                </Button>
-                <Button type="button" onClick={() => {
-                  printRaporPDF(selectedStudentForRapor, raporPaperSize);
-                  setSelectedStudentForRapor(null);
-                }} className="bg-rose-600 hover:bg-rose-700 text-white flex items-center gap-1.5 font-bold">
-                  <Printer size={14} /> Download Rapor
-                </Button>
-              </div>
             </div>
           </div>
         </div>
[diff_block_end]

Please note that the above snippet only shows the MODIFIED lines from the last change. It shows up to 3 lines of unchanged lines before and after the modified lines. The actual file contents may have many more lines not shown.

We did our best to apply changes despite some inaccuracies. Double check if the edit applied is what you intended.